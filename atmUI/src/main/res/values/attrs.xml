<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="CashUI">

        <attr name="rippleColour" format="reference" />
        <!--Buttons/Actions-->
        <attr name="primaryActionBorder" format="reference"/>
        <attr name="primaryActionTextColour" format="reference"/>
        <attr name="primaryActionBackground" format="reference"/>

        <attr name="primaryActionHighlightedBorder" format="reference"/>
        <attr name="primaryActionHighlightedTextColour" format="reference"/>
        <attr name="primaryActionHighlightedBackground" format="reference"/>

        <attr name="secondaryActionBorder" format="reference"/>
        <attr name="secondaryActionTextColour" format="reference"/>
        <attr name="secondaryActionBackground" format="reference"/>
    </declare-styleable>
    <declare-styleable name="DropAnimationView">
        <!-- Animation rate, use px/s as a unit -->
        <attr name="rate" format="integer" />

        <!-- Minimum size of the falling view -->
        <attr name="minSize" format="dimension" />

        <!-- Maximum size of the falling view -->
        <attr name="maxSize" format="dimension" />

        <!-- The bigger view should be falling fast -->
        <attr name="largePercent" format="fraction" />

        <!-- Whether enable horizontal translation, default false -->
        <attr name="xAnimate" format="boolean" />

        <!-- Whether enable vertical translation, default true  -->
        <attr name="yAnimate" format="boolean" />

        <!-- Whether enable rotation, default false -->
        <attr name="rotationAnimate" format="boolean" />
    </declare-styleable>
</resources>